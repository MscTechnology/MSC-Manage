query GetUserDetail($prmId: Long!){
  usersById(where: {id:{eq:$prmId}}) {
    name
    surname
    username
    password
    id
    status
    cityid
    districtsid
    usersmovements {
      transactiondate
      entrytime
      exittime
    }
    
  }
}